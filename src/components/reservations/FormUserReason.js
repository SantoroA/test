import React from 'react';

const FormUserReason = ({ step, nextStep, previousStep, reasonForVisit, setReasonForVisit }) => {
	return (
		<div>
			<h1>WHat is the reason for this visit?</h1>
		</div>
	);
};

export default FormUserReason;
