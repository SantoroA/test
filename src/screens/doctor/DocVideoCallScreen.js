import React from 'react';
import DocLayoutContainer from '../../components/layout/DocLayoutContainer';
import Typography from '@material-ui/core/Typography';

const DocVideoCallScreen = () => {
	return (
		<DocLayoutContainer>
			<Typography variant="h4">Doctor Video Call</Typography>
		</DocLayoutContainer>
	);
};

export default DocVideoCallScreen;
